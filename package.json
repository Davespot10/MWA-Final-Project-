{
    "name":"Lost-and-found",
    "version":"1.0.0",
    "scripts": {
        "start":"cd backend && node app.js",
        "prebuild":"cd backend && npm run build",
        "build":"cd frontend && npm run build && ng serve"
    }

}